import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  'thumb-cover': {
    'display': 'block',
    'height': [{ 'unit': 'px', 'value': 115 }],
    'overflow': 'hidden',
    'textAlign': 'center'
  }
});
